# Repository Guidelines

## 프로젝트 구조 및 모듈 구성

현재 저장소는 구현 전 단계이며, 핵심 문서는 `screen_golf_service_design.md`, 원천 엑셀 자료는 `raw_data.zip`에 보관되어 있습니다. 구현을 시작하면 설계서의 권장 구조에 맞춰 `frontend/`, `backend/`, `nginx/`, `db/`, `uploads/`, `backups/`, `docs/`를 분리하세요. 서비스 코드는 프론트엔드와 백엔드/API를 명확히 나누고, 운영 문서와 스키마 초안은 `docs/` 또는 `db/`에 둡니다.

## 빌드, 테스트, 개발 명령

아직 실행 가능한 애플리케이션은 없습니다. Docker Compose 환경이 추가되면 저장소 루트에서 다음 명령을 기준으로 관리하세요.

- `docker compose up --build`: 전체 서비스를 빌드하고 로컬 실행
- `docker compose down`: 실행 중인 컨테이너 정리
- `pytest`: FastAPI 백엔드 테스트 실행 시 사용
- `npm test`: 프론트엔드 테스트 실행 시 사용
- `npm run lint`: 프론트엔드 린트 검사 시 사용

실제 명령은 각 모듈의 `README.md`나 패키지 설정에 맞게 갱신하세요.

## 코딩 스타일 및 명명 규칙

파일과 모듈 이름은 영어 소문자와 하이픈 또는 언더스코어를 사용합니다. 예: `membership-products`, `audit_logs`. 사용자에게 보이는 문구는 운영자가 이해하기 쉬운 한글을 우선합니다. 설정값은 코드에 직접 쓰지 말고 `.env`와 환경변수로 관리하세요. 프론트엔드는 큰 글씨, 단순한 버튼, 검색 중심 화면을 우선하고, 백엔드는 CRUD, 이력 저장, 공통 응답 형식을 일관되게 유지합니다.

## 테스트 지침

구현 후에는 회원 검색, 매출 등록, 정기권/쿠폰 자동 생성, 회원별 매출 연결, 환불, 문자 발송 전 확인, 업로드 파일 검증, 감사 로그 저장을 우선 테스트하세요. 백엔드는 API 단위 테스트와 데이터 검증 테스트를 작성하고, 프론트엔드는 주요 운영 흐름 중심의 화면 테스트를 추가합니다. 테스트 파일명은 기능명을 드러내도록 작성하세요. 예: `test_member_search.py`, `SaleEntry.test.tsx`.

## 커밋 및 Pull Request 지침

현재 Git 이력은 `Initial commit`만 있으므로, 앞으로는 짧은 명령형 커밋 메시지를 사용하세요. 예: `Add member schema draft`, `Implement sales summary API`. PR에는 변경 목적, 주요 화면 또는 API, 테스트 결과, 관련 이슈를 포함하세요. UI 변경은 스크린샷을 첨부하고, DB나 `.env` 변경이 있으면 마이그레이션 및 운영 영향도 함께 적습니다.

## 보안 및 설정 팁

`.env`, DB 비밀번호, SMS API 키, 백업 파일, 업로드 문서, 압축 해제한 개인정보성 엑셀 파일은 커밋하지 마세요. 외부 공개 시 HTTPS, VPN 또는 IP 제한을 우선 검토합니다. 삭제 기능보다 비활성화와 감사 로그를 기본값으로 두고, 백업 및 복구 절차는 구현 초기부터 문서화하세요.
